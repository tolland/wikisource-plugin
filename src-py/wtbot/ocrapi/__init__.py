"""Reusable OCR service, upstream client, and engine catalog helpers.

The HTTP routes and persisted configuration belong to wtbot; see
``wtbot.api.ocr``. This package has no standalone FastAPI application.
"""
